# MyStats 프로덕션 고도화 계획서 (Production Upgrade Plan)

> 목적: `mystats`를 **스토어 없이도 “프로덕션급 PWA 앱”**으로 런칭/운영 가능하게 만들고, 사용자가 늘어도 **안정성·비용·보안·데이터 신뢰성**을 유지한다.
>
> 기준일: 2026-01-24 (최종 업데이트: 2026-01-30)
> 현재 배포(데모): https://mystats-eta.vercel.app/
> 강화 전략(Execution): `docs/STRENGTHENING_STRATEGY.md`

---

## 0) TL;DR (요약)

- **단기(1~2주)**: 품질/운영 기반을 깔아 “깨져도 바로 알 수 있고”, “재현 가능하고”, “복구가 쉬운” 상태로 만든다.  
  (CI, 테스트 최소세트, Sentry, 데이터/백업 UX, PWA 업데이트 UX)
- **중기(2~6주)**: “어디서나” 경험을 위해 **옵션형 로그인 + 클라우드 동기화**를 붙인다(로컬 퍼스트 유지).  
  (Supabase Auth/DB + 동기화 큐 + 충돌 해결)
- **장기(6주~)**: “아무나 AI를 바로 쓰는” 구조가 필요하면 **서버리스 AI 프록시 + 비용 방어(요금제/쿼터/레이트리밋)**로 확장한다.  
  (Free=BYOK, Pro=내장 AI)

---

## 1) 현재 상태 (As-Is)

### 1.1 제품/배포
- Vercel + GitHub 연동 배포
- SPA 라우팅 + PWA 설치 지원(서비스워커/manifest)
- 스토어 배포 없이도 iOS/Android에서 “홈 화면에 추가/앱 설치” 가능

### 1.2 데이터/저장소
- **Local-first**: IndexedDB(idb) + fallback(localStorage/메모리)
- Export/Import(백업/복원) 기능 있음

### 1.3 AI
- BYOK(Bring Your Own Key): 사용자가 Settings에 API 키를 넣고 브라우저에서 직접 각 Provider 호출
- Provider: Gemini/OpenAI/Claude/Grok 선택 가능

### 1.4 메모리(memU)
- **내장(Embedded) memU 엔진**을 MyStats 내부에 포함
  - 저널(IndexedDB/fallback)을 메모리 소스로 사용
  - retrieve(top-K) / check-similar(유사도) 제공
  - Strategy 생성 시 memU 컨텍스트를 자동 주입 가능
- **서버(API) 모드** 옵션도 유지(로컬 memU 서버가 있으면 연결 가능)

---

## 2) 목표 (To-Be)

### 2.1 제품 목표
1. **설치/실행이 빠르고**(PWA), **오프라인에서도 기본 기능이 동작**
2. 데이터 손실/중복에 강하고, 사용자가 스스로 복구 가능(백업/복원/동기화)
3. 장애가 나면 “원인/영향/재현”이 가능한 운영 레벨(관측/로그/리포팅)
4. (선택) 누구나 AI 기능을 바로 쓰게 하되, 비용 폭탄 없이 운영(서버리스 프록시 + 요금제)

### 2.2 성공 지표(예시)
- 크래시/Unhandled error 비율 ↓, Sentry에서 원인 분류 가능
- “데이터 복구 성공률” ↑ (백업/복원 + 동기화)
- PWA 설치 전환율(Install prompt, iOS 안내 클릭) ↑
- AI 비용(토큰/요청) 예측 가능 + 과금 단위 정의 가능

---

## 3) 핵심 의사결정(Decision)

### 3.1 앱 형태
- **PWA 우선** (스토어 불필요)  
  - 운영/배포가 빠르고, 한 코드베이스로 웹+모바일을 커버
  - 단, iOS Safari의 PWA 제약(백그라운드 제한 등)은 UX로 보완

### 3.2 데이터 전략
- **Local-first 유지** + (옵션) **로그인 기반 클라우드 동기화**
  - 데모/라이트 사용자는 로그인 없이도 사용 가능
  - “기기 바뀌어도 동일 데이터”가 필요하면 로그인/동기화 제공

### 3.3 AI 전략(비용/보안)
- 기본은 BYOK 유지(서버 비용 0)
- 대중 런칭/유료화 시:
  - **서버리스 프록시에서만 LLM 호출**
  - 요금제/쿼터/레이트리밋으로 비용을 통제

---

## 4) 메모리 시스템(memU) 업데이트 상세

### 4.1 왜 내장(Embedded)인가?
- 런칭 초기에는 “서버를 운영하지 않아도” 메모리 검색/유사도 기능을 제공하는 게 중요
- 사용자 입장에서 설정/의존성이 줄어든다(설치/운영 장벽 ↓)

### 4.2 구성 요소
- `Embedded engine`:
  - 데이터 소스: `Journal`(IndexedDB) + fallback
  - 동작: 텍스트 → 해시 기반 벡터화 → 코사인 유사도
  - 제공 API: `retrieve`, `check-similar`
- `Server (API) engine`:
  - 기존 `memU` 서버와 REST로 통신(`/health`, `/retrieve`, `/check-similar`, `/items`)
  - Strategy에서 `project-registry`까지 포함 검색 옵션 제공

### 4.3 현재 한계/의도된 제약
- Embedded는 “프로젝트 레지스트리” 같은 외부 스코프 검색을 하지 않음(프라이버시/스코프 안전)
- 임베딩 품질은 “경량/오프라인/비용 0”에 최적화되어 있고, 필요 시 향후 고급 임베딩으로 업그레이드 가능

### 4.4 다음 개선(프로덕션)
- ✅ Web Worker로 임베딩/검색을 분리(긴 저널에서도 UI 프리징 방지)
- 벡터 캐싱/증분 인덱싱(매번 전체 재계산 방지)
- 저장 시 “유사 기록 경고/병합” UX(데이터 품질 ↑)

---

## 5) 작업 스트림(Workstreams) & 실행 순서

> 원칙: “사용자 체감 + 운영 리스크”가 큰 것부터 한다.

### WS-A) 운영/품질 기반 (가장 먼저)
**목표:** 깨지면 바로 감지/재현/복구 가능

1) CI 파이프라인 (GitHub Actions) — **✅ 완료 (v1.1.2)**
- `lint`, `build`, `test` 필수 체크 (`.github/workflows/ci.yml`)
- PR 템플릿/체크리스트 (`.github/PULL_REQUEST_TEMPLATE.md`)
- Issue 템플릿: bug report + feature request (`.github/ISSUE_TEMPLATE/`)

2) 테스트 최소세트 — **✅ 완료**
- ✅ 유닛(Vitest): memU embedded (10), utils (8), fallback (59), db (3), cloudSync (3), ai-provider (3) — 총 86 tests
- 🔶 e2e(Playwright): 저널 저장/히스토리 유지, 백업 Export→Reset DB→Import 복원 (스모크 2개)
  - ⬜ 추가 후보: 전략 생성, Settings 저장, Cloud Sync 로그인/동기화(환경 의존)

3) 에러/성능 관측 — **✅ 완료 (v1.2.2)**
- Sentry 도입 완료 (lazy-loaded, `VITE_SENTRY_DSN`)
- "리포트 보내기" UX: 크래시 화면에서 디버그 스냅샷 복사 기능 (v1.1.2)

**완료 기준(DoD)**
- ✅ main에 머지되면 빌드/린트/테스트가 자동으로 돌고, 실패 시 배포가 막힘
- ✅ 데모에서 주요 플로우 1~2개는 e2e로 보호됨

---

### WS-B) 데이터 신뢰성/복구 (사용자 체감 큼)
**목표:** "데이터가 내 자산"이라는 신뢰 확보

1) 백업/복원 강화 — **✅ 완료 (v1.2.3)**
- ✅ 백업 포맷 버전 관리(앱 버전 + DB 스키마 버전 포함)
- ✅ 복원 확인 다이얼로그에서 머지 동작 설명 (번역 지원)

2) 마이그레이션/스키마 관리 — **✅ 완료**
- ✅ IndexedDB VersionError 자동 복구 (v1.2.5)
- ✅ DB 초기화/복구 자동화 (useDbRecovery hook, fallback → DB 자동 마이그레이션)
- ✅ 스키마 마이그레이션 경로 문서화: `docs/DB_MIGRATIONS.md`

3) 프라이버시 UX — **🔶 일부 완료**
- ✅ "이 데이터는 기기별 저장" 안내 (Dashboard storage mode badge)
- ⬜ (선택) 로컬 암호화(패스프레이즈)

---

### WS-C) 성능/UX (PWA의 완성도)
**목표:** 모바일에서 빠르고 부드럽게

1) memU embedded 성능 — **🔶 대부분 완료**
- ✅ 임베딩 캐싱 (v1.2.3): per-entry embedding cache로 반복 검색 가속
- ✅ Worker 전환 (UI 프리징 방지): Web Worker 오프로딩
- 🔶 검색 결과 하이라이트/근거 UX: 근거(소스) 표시는 완료, 하이라이트는 추후

2) 번들/로딩 — **✅ 완료**
- ✅ 페이지별 lazy-load + code splitting (App.tsx)
- ✅ react-markdown 동적 임포트 (v1.2.6)
- ✅ async waterfall 제거 - Promise.all 병렬화 (v1.2.6)
- ✅ useMemo로 불필요한 재계산 방지 (v1.2.6)
- ✅ content-visibility CSS로 스크롤 렌더링 최적화 (v1.2.6)
- ✅ "업데이트 있음" 토스트 (PwaUpdatePrompt, v1.1.3)

3) 접근성(A11y) — **🔶 일부 완료 (v1.2.6)**
- ✅ 폼 라벨/aria: Settings htmlFor/id 연결, ARIA listbox/option, aria-haspopup/expanded
- ✅ 키보드: Journal Ctrl+Enter 저장 단축키
- ✅ 내비게이션: Shell aria-current="page"
- ⬜ 색 대비 감사, 스크린 리더 전체 테스트

---

### WS-D) "어디서나" 동기화 — **✅ 완료 (v1.2.0)**
**목표:** 로그인하면 어떤 기기에서도 같은 데이터

구현된 설계: **옵션형 Sync (Beta)**
- ✅ 기본: 로컬 저장(즉시, 로그인 불필요)
- ✅ 백그라운드: 데이터 변경 시 자동 동기화 (cloudSyncManager)
- ✅ Auth: Supabase Auth (OAuth + Email/Password)
- ✅ DB: Supabase Postgres (RLS 적용)
- ✅ SQL 마이그레이션 + 환경변수 설정 스크립트 제공
- ✅ 설정 가이드: `docs/CLOUD_SYNC.md`

남은 개선:
- ⬜ 충돌 해결: 현재 last-write-wins → 필드 단위 머지 고려
- ⬜ 동기화 상태 상세 UI (진행률, 충돌 알림)

---

### WS-E) AI를 대중에게 열기(유료화/비용 방어) — 옵션
**목표:** 사용자가 API 키 없이도 AI 기능 사용

권장 모델
- Free: BYOK + 로컬 기능
- Pro: 내장 AI(우리 키) + 월 사용량 제한 + 우선 모델

필수 방어 장치
- 서버리스 프록시에서만 호출(키는 서버에만)
- rate limit/쿼터(일/월), 입력 길이 제한
- abuse 탐지(짧은 시간 내 반복 호출 등)

---

## 6) 릴리즈/버전 전략

- SemVer: `MAJOR.MINOR.PATCH`
- 변경 기록: `CHANGELOG.md` 유지
- GitHub Release: 태그(`vX.Y.Z`) + 릴리즈 노트 자동화(추후)
- Vercel: main=production, PR=preview 배포 유지

---

## 7) 로드맵(제안)

### Milestone 1 (v1.2.x): "운영 가능한 PWA" — **✅ 완료**
- ✅ CI (GitHub Actions: lint + test + build)
- ✅ 최소 테스트 (Vitest 86 tests)
- ✅ Sentry 에러 추적
- ✅ 백업/복원 UX 강화 (버전 관리, 머지 설명)
- ✅ memU embedded 캐싱
- ✅ UI 성능 최적화 (Promise.all, useMemo, lazy import, content-visibility)

### Milestone 2 (v1.3.x): "어디서나 동일 데이터(옵션)" — **🔶 대부분 완료**
- ✅ Auth + Cloud Sync (Supabase, opt-in)
- ✅ 접근성(A11y) 개선 — Settings ARIA, Shell aria-current, Journal Ctrl+Enter (v1.2.6)
- ✅ 테스트 확대 — db/cloudSync/ai-provider 유닛 추가 (총 86개, v1.2.6+)
- ✅ 오류 처리/타입 강화 (v1.2.6)
- ⬜ 충돌 해결 고도화 (필드 단위 머지)
- ⬜ 동기화 상태 상세 UI
- 🔶 e2e 테스트 (Playwright) — 스모크 2개
- ✅ Settings.tsx 분할 (4개 컴포넌트)

### Milestone 3 (v2.0): "유료/대중 런칭(선택)" — **⬜ 미착수**
- ⬜ 서버리스 AI 프록시
- ⬜ 요금제/쿼터/결제

---

## 8) 코드 품질 심층 분석 결과 (2026-01-29)

> v1.2.6 기준 코드베이스 전수 분석. 우선순위별 개선 항목 정리.

### 8.1 테스트 커버리지 갭 — 🔴 높음

현재 (v1.2.6+): 6 파일, 86 테스트 (Vitest) + e2e 2개 (Playwright)

| 모듈 | 테스트 | 위험도 | 비고 |
|------|--------|--------|------|
| `db/db.ts` (381줄) | ✅ 3개 | 🟡 | initDB 복구/upgrade, importAllData 정상화(타임스탬프/검증) |
| `db/fallback.ts` (289줄) | ✅ 59개 | ✅ | CRUD, edge cases, limits, dedup, skill merging |
| `lib/cloudSync.ts` (362줄) | ✅ 3개 | 🔴 | 동기화/충돌 해결 로직은 여전히 얕은 커버리지 |
| `lib/cloudSyncManager.ts` (57줄) | ⬜ 없음 | 🟡 | 타이머/이벤트 핸들러 |
| `lib/ai-provider.ts` (395줄) | ✅ 3개 | 🟡 | storage 안전/프라이빗 모드 시뮬레이션 |
| `lib/translations.ts` | ⬜ 없음 | 🟢 | 번역 키 일관성 검증 |
| Pages (5개) | 🔶 e2e 2개 | 🟡 | Journal 저장/히스토리, Backup Export/Import 복원 |

**권장 순서**: cloudSync.ts(충돌/리트라이) → db.ts(마이그레이션/복구) → pages/e2e 확대 → cloudSyncManager.ts

### 8.2 Settings.tsx 분할 — 🟡 중요

현재: **✅ 분할 완료** — Settings 페이지는 카드 단위로 구성하고, 세부 구현은 `src/pages/settings/*`로 분리

```
src/pages/Settings.tsx
src/pages/settings/
├── AISettingsCard.tsx
├── MemuSettingsCard.tsx
├── CloudSyncCard.tsx
└── DataManagementCard.tsx
```

**효과**: 유지보수성 향상, 코드 리뷰 용이, 번들 분할 가능성

### 8.3 접근성(A11y) 결함 — 🔴 높음

| 위치 | 이슈 | 수정 방향 |
|------|------|----------|
| Settings.tsx (554-843) | ✅ 해결 (v1.2.6) | htmlFor/id 연결 + aria-haspopup/expanded + role="listbox"/option |
| Settings.tsx (573-598) | ✅ 해결 (v1.2.6) | ARIA listbox 패턴 적용 |
| Journal.tsx (410-416) | ✅ 해결 (v1.2.6) | Ctrl+Enter onKeyDown 핸들러 추가 |
| Shell.tsx (25-42) | ✅ 해결 (v1.2.6) | aria-current="page" 추가 |

### 8.4 오류 처리 일관성 — 🟡 중요

| 위치 | 이슈 |
|------|------|
| cloudSyncManager.ts:13-18 | ✅ 해결 (v1.2.6) — console.debug 로깅 추가 |
| Journal.tsx:54-62 | ✅ 해결 (v1.2.6) — VersionError, QuotaExceededError 추가 |
| App.tsx:18-31 | 초기화 실패 시 사용자 알림 없음 → DB 실패 이벤트 발행 |

### 8.5 타입 안전성 — 🟡 중요

| 위치 | 이슈 | 수정 방향 |
|------|------|----------|
| db.ts:224 | `as unknown as IDBPDatabase<any>` | 레거시 DB 스키마 인터페이스 정의 |
| db.ts:287 | ✅ 해결 (v1.2.6) — 구체적 union 타입으로 교체 |
| tsconfig.json:6 | ✅ 해결 (v1.2.6) — ES2023으로 변경 |
| Profile.tsx:37 | ✅ 해결 (v1.2.6) — 미사용 import 제거 |

### 8.6 성능 미세 개선 — 🟢 낮음

| 위치 | 이슈 | 수정 방향 |
|------|------|----------|
| Shell.tsx:49-55 | `navItems` 배열 매 렌더 재생성 | `useMemo([t])` 래핑 |
| cloudSync.ts:220-227 | ✅ 해결 — 네트워크 오류 시 재시도 추가 | `syncNowWithRetry()` (지수 백오프 3회) |
| memU Worker 전환 | ✅ 해결 — 긴 저널에서 UI 프리징 완화 | Web Worker로 임베딩/검색 오프로딩 |

---

## 9) 다음 액션 (Next Steps)

> Milestone 1 완료, Milestone 2 대부분 완료. 아래는 코드 분석 기반 우선순위 정리.

### P0 — 즉시 (안정성/접근성) — ✅ 완료 (v1.2.6)
1) ✅ **tsconfig lib ES2023 업그레이드**: `toReversed()` 런타임 호환성 보장
2) ✅ **테스트 확대**: fallback.ts 59개 + db/cloudSync/ai-provider 유닛 테스트 추가 (총 86개)
3) ✅ **접근성(A11y)**: Settings 폼 라벨/ARIA, Shell aria-current, Journal Ctrl+Enter

### P1 — 단기 (품질/구조) — 🔶 대부분 완료
4) ✅ **Settings.tsx 분할**: 4개 컴포넌트로 책임 분리
5) ✅ **오류 처리 통일**: isDbFailure 확대, cloudSyncManager console.debug 로깅
6) ✅ **미사용 import 정리**: Profile.tsx `clearFallbackData` 제거
7) ✅ **타입 강화**: db.ts의 `any` → 구체적 타입 교체

### P2 — 중기 (고도화)
8) 🔶 **e2e 테스트**: 스모크 2개 완료(저널/백업), 전략 생성/Settings 저장은 추후
9) 🔶 **동기화 고도화**: 네트워크 재시도 ✅, 충돌 해결 UI ⬜
10) ✅ **memU Worker 전환**: 긴 저널에서 UI 프리징 방지
11) ✅ **DB 마이그레이션 문서화**: `docs/DB_MIGRATIONS.md`

### P3 — 제품 진화 (신규 기능)
> 상세: `docs/CREATIVE_IDEAS.md`
12) Shadow Profile — "안 쓴 것" 감지를 통한 회피 영역 분석
13) Paradox Portfolio — 모순되는 특성을 경쟁 우위로 재해석
14) Temporal Identity Drift — 시간축 정체성 변이 추적
15) Decision Oracle — 프로필 기반 결정 시뮬레이션
16) Cognitive Fingerprint — 글쓰기 패턴으로 사고방식 분석
17) Adversarial Self-Interview — AI가 방어 메커니즘을 겨냥하는 질문 설계
18) Future Self Letter — 미래의 나가 현재의 나에게 쓰는 편지
