# Public client-side env vars (safe to commit)

# Supabase (Cloud Sync)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: memU server (only used when memU Engine = Server/API)
MEMU_API_URL=http://localhost:8100

# Optional: Sentry (error reporting)
VITE_SENTRY_DSN=
